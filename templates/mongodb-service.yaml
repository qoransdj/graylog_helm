apiVersion: v1
kind: Service
metadata:
  name: mongodb-svc
  labels:
    app: graylog
    component: mongodb
spec:
  clusterIP: None
  ports:
    - name: "27017"
      port: 27017
      targetPort: 27017
  selector:
    app: graylog
    component: mongodb