apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    component: graylog-claim0
  name: graylog-claim0
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 500Mi